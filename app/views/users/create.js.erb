$('table tbody').append("<%= j(render 'row', user: @user) %>");
<% if @user.errors.any? %>
  $('.new_user').effect('highlight', { color: '#FF0000'}, 1000); // for highlighting
  // or add here whatever jquery response you want to have to your views.
<% end %>